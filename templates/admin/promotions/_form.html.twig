{{ form_start(promotionsForm) }}
    {{ form_row(promotionsForm.products) }}
    {{ form_row(promotionsForm.pourcentage) }}
    {{ form_row(promotionsForm.date_deb) }}
    {{ form_row(promotionsForm.date_fin) }}

    <button type="submit" class="btn btn-primary">{{ button_label|default('Ajouter') }}</button>
{{ form_end(promotionsForm) }}